{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Launch Firefox in Debug Mode",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}"
        },
    
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Chrome in Debug Mode",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}