# to change the windows default font, replace 'Ubuntu' with your desired
# font family in 'change.windows.font.reg' file and execute it.

# to restore the windows default font, execute 'restore.default.windows.font.reg' file.
