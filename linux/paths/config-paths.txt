# list of repositories:

/etc/apt/sources.list
/etc/apt/sources.list.d

# dns servers in use:

/etc/resolv.conf

# dns server switch config:

/etc/nsswitch.conf

# host names:

/etc/hosts

# nginx configs:

/etc/nginx/conf.d

# crontabs:

/etc/cron.d

# ssh daemon:

/etc/ssh/sshd_config -> defaults
/etc/ssh/sshd_config.d/ -> custom
