# NOTE:
# this will only work on ubuntu 24.04 and newer.

# install virtualenv:

sudo apt-get install virtualenv

# then install pipenv using any version of python which is
# not included in your distro by default (ex. 3.9):

python3.13 -m pip install --user pipenv

# upgrade pip

python3.13 -m pip install --upgrade pip
