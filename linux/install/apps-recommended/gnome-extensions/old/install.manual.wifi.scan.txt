# newer gnome versions perform the Wi-Fi scanning every 15 seconds by default
# and there is no need to this extension anymore.

# go to 'https://extensions.gnome.org/extension/905/refresh-wifi-connections/' (Gnome <= 42)
# page and toggle the install icon.
