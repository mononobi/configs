# installation:
sudo /sbin/modprobe tun
sudo apt-get install openconnect

# connection:
sudo openconnect {SERVER_ADDRESS}

# if system traffic not gone through vpn:
sudo apt-get install vpnc vpnc-scripts
sudo openconnect {SERVER_ADDRESS} --script=/etc/vpnc/vpnc-script
