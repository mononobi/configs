1. `.vscode` folder should be placed into your project's root directory (where `.gitignore` is located).

2. `.env` file should be placed into your project's root directory (where `.gitignore` is located).

3. make sure to change paths and other stuff in different files to your correct environment.
